{
  "name": "turism",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @turism/web dev",
    "build": "pnpm -r --filter './packages/**' --filter './apps/**' build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=20" }
}
